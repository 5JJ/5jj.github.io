export const DATE_HEIGHT = 44;
export const DATE_LENGTH = 6;
export const MIDDLE_INDEX = Math.floor(DATE_LENGTH / 2);
export const MIDDLE_Y = -DATE_HEIGHT * MIDDLE_INDEX;
export const DIRECTION = {
  DOWN: 1,
  UP: -1,
} as const;
